functions:
  fc_str_json_todict:                                                                   # The name which will be used in the GCode to call the function. Must be unique.
    path: /home/pi/klipper_config/ACTIVE/CONFIG/KLIPPER_FUNCTIONS/strjson_to_dict2.py   # Location of Python script. Must be an absolute path
    function: strjson_to_dict2                                                          # The actual name of the Python function
  fc_str_to_dict:                                                                       # The name which will be used in the GCode to call the function. Must be unique.
    path: /home/pi/klipper_config/ACTIVE/CONFIG/KLIPPER_FUNCTIONS/string_to_dict2.py    # Location of Python script. Must be an absolute path
    function: string_to_dict2                                                           # The actual name of the Python function
filters:
  fr_str_json_todict:                                                                   # The name which will be used in the GCode to call the function. Must be unique.
    path: /home/pi/klipper_config/ACTIVE/CONFIG/KLIPPER_FUNCTIONS/strjson_to_dict2.py   # Location of Python script. Must be an absolute path
    function: strjson_to_dict2                                                          # The actual name of the Python function
  fr_str_to_dict:                                                                       # The name which will be used in the GCode to call the function. Must be unique.
    path: /home/pi/klipper_config/ACTIVE/CONFIG/KLIPPER_FUNCTIONS/string_to_dict2.py    # Location of Python script. Must be an absolute path
    function: string_to_dict2                                                           # The actual name of the Python function
